//-------------------------------------------------------
// This is a central place for any config values
// that different parts of caribou might need.
//-------------------------------------------------------

(function(global) {
  global = global || {};
  global.caribou = global.caribou || {};
  global.caribou.config = {
    "pages": {
      "results": {
        "page-size": 20
      }
    }
  };
})(window);
