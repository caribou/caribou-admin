(ns caribou.admin.controllers.content.abstract_models
  (:use caribou.app.controller))
  
(defn index
  [params]
  (render params))

(defn view
  [params]
  (render params))

(defn edit
  [params]
  (render params))

(defn create
  [params]
  (render params))

(defn update
  [params]
  (render params))

(defn destroy
  [params]
  (render params))
